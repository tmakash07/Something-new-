{
  "name": "telegram-ai-quiz-bot",
  "version": "1.0.0",
  "description": "Production-ready Telegram Quiz Bot powered by Google Gemini and Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@google/genai": "^0.1.1",
    "grammy": "^1.34.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.101.0"
  }
}
